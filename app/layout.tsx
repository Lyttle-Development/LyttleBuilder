import type { Metadata } from "next";
import { Geist, Geist_Mono } from "next/font/google";
import "./globals.css";

const geistSans = Geist({
  variable: "--font-geist-sans",
  subsets: ["latin"],
});

const geistMono = Geist_Mono({
  variable: "--font-geist-mono",
  subsets: ["latin"],
});

export const metadata: Metadata = {
  title: "Lyttle Builder - Build React Apps with Ease",
  description: "Lyttle Builder is a low-code platform that allows you to create React applications quickly and easily. With our intuitive drag-and-drop interface, you can design your app visually and export clean, production-ready code.",
  keywords: ["low-code", "react builder", "drag-and-drop", "visual development", "react applications"],
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html lang="en">
      <body
        className={`${geistSans.variable} ${geistMono.variable} antialiased`}
      >
        {children}
      </body>
    </html>
  );
}
